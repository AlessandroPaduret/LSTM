[project]
name = "LSTM"
version = "1.0.0"
description = "..."
readme = "README.md"
authors = [
    { name = "Alessandro Paduret", email = "alessandro.paduret@studenti.unipd.it" },
]
requires-python = ">= 3.12"
dependencies = [
    "polars",
    "torch"
]
